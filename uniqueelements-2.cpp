//doing the question through the bit masking approach rather than moving through the brute force approach
#include<bits/stdc++.h>
using namespace std;
void findunique2(int *arr,int n){
    int result=0;
    for(int i=0;i<=n-1;i++){
        result=result^arr[i];
        }
        //finding the rightmost set bit in the bitset 
        int i=0;
}
int main(){
    int arr[]={1,3,5,6,3,2,1,2};
    int size=sizeof(arr)/sizeof(arr[0]);



    return 0;
}